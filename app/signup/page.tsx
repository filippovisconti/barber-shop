'use client';

import SignUpForm from '@/components/SignUpForm/SignUpForm';
import { Container } from '@mantine/core';

const page = () => (
        <main>
            <Container>
                <SignUpForm />
            </Container>
        </main>
    );

export default page;
